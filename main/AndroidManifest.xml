<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.esa.www.keepass">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bean.activity_regi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bean.activity_login"
            android:label="Anmelden"/>
        <activity android:name=".bean.MainActivity"
            android:label="Übersicht"/>
        <activity android:name=".bean.activity_add_edit_note"
        android:label="Eintrag bearbeiten"/>
        <activity android:name=".bean.activity_add_create_note"
            android:label="Eintrag erstellen"/>
    </application>
</manifest>